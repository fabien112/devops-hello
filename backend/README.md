# Backend — devops-hello

API Spring Boot 3 (Java 17) : Hello + CRUD Clients (JPA / PostgreSQL).

## Commandes

```bash
# Tests
.\mvnw.cmd clean verify

# Lancer (PostgreSQL doit tourner)
.\mvnw.cmd spring-boot:run

# Image Docker
docker build -t devops-backend:1.0.0 .
```
